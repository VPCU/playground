extends layout

block content
    .row.align-items-center
        form(method='post')
            .mb-3
                label.form-label(for='username1') 用户名
                input#username1.form-control(type='text' name='username')
            .mb-3
                label.form-label(for='InputPassword1') 密码
                input#InputPassword1.form-control(type='password' name='password')
            button.btn.btn-primary(type='submit') 登录